SpaceX Stats
Installation
Prereqs
Node >= v8.0.0
Yarn >= v1.13.0
Gatsby CLI >= v2.6.7: run npm install -g gatsby-cli
Setup instructions
In the project directory, run npm install command.
Run gatsby develop.
Open http://localhost:8000/.
...
Profit!
Deployment
Push on the master branch.
